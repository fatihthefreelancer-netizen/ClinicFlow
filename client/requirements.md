## Packages
recharts | For the analytics dashboard charts
date-fns | For date formatting and manipulation
framer-motion | For smooth page transitions and UI animations

## Notes
WebSocket connects to window.location.protocol + "//" + window.location.host + "/ws"
User roles are 'doctor' and 'assistant'
Assistant can only edit basic fields; Doctor can edit all fields including Price and Next Step
Live Board updates in real-time via WebSocket
